#pragma once

// Host stand-in: SettingsActivity holds an OptionPopup member by value, but
// the suite never instantiates the activity. A trivial complete type is all
// the class body needs.

class OptionPopup {};
