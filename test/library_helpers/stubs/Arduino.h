#pragma once

// Minimal Arduino surface for the library-helpers host suite: just the String
// class the stubbed Epub/Fb2/Txt/Xtc headers name. ButtonNavigator's hold
// timing goes through the platform seam, not Arduino millis().

#include <WString.h>
