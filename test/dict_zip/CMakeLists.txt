# DictZip (.dict.dz random-access gzip) reader suite. Compiles the production
# reader plus the real uzlib-backed InflateReader it inflates through, over the
# stdio HalStorage stub shared with test/dictionary. Fixtures are generated by
# scripts/generate_test_dict.py and checked in under test/dict_common/resources.
add_executable(DictZipTest
  DictZipTest.cpp
  ${REPO_ROOT}/test/support/AllocCounter.cpp
  ${REPO_ROOT}/src/util/DictZip.cpp
  ${REPO_ROOT}/lib/InflateReader/InflateReader.cpp
  ${REPO_ROOT}/lib/uzlib/src/tinflate.c
  ${REPO_ROOT}/test/dict_common/UzlibChecksumLinkStubs.c
)

target_include_directories(DictZipTest PRIVATE
  ${REPO_ROOT}/test/dict_common/stubs
  ${REPO_ROOT}/lib/InflateReader
  ${REPO_ROOT}/lib/uzlib/src
  ${REPO_ROOT}/lib/Memory
)

# Third-party C decompressor: not held to the suite's warning bar.
set_source_files_properties(${REPO_ROOT}/lib/uzlib/src/tinflate.c PROPERTIES COMPILE_OPTIONS "-w")

target_compile_definitions(DictZipTest PRIVATE
  DICT_RESOURCES_DIR="${REPO_ROOT}/test/dict_common/resources"
)

target_link_libraries(DictZipTest PRIVATE
  crosspoint_test_common
  GTest::gtest_main
)

gtest_discover_tests(DictZipTest)
